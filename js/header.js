/* 
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
/*var defaultHeaderFontSize;
var bodyWidthCutoff = 400;
var scale;

function calcHeaderTextSize()
{
    if($("body").innerWidth() < 400)
        $("#header h1").css("font-size", scale + "%");
    else
        $("#header h1").css("font-size", defaultHeaderFontSize + "px");
}

$(document).ready(function(){
    defaultHeaderFontSize =  parseFloat($("#header h1").css("font-size"));
    scale = 100 * (bodyWidthCutoff / defaultHeaderFontSize);  //100vw = 1px
    console.log(defaultHeaderFontSize + " \ " + scale);
    $(window).resize(calcHeaderTextSize);
    calcHeaderTextSize();
});*/